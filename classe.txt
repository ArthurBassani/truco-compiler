// CLASSE

void modAcesso() :
{}
{
  ( modPrivado()
  | modPublico()
  )
}

void declClasse(): { } { < DECLASSE > }

void deClasse() :
{}
{
  declClasse() ident() abreChave()
	( [ modAcesso() ] ( declVar() | declFuncao() ) )*
  fechaChave()
}